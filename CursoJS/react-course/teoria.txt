React:
    - Biblioteca utilizada para criação de interface de usuário
        - interface Web
        - interface Mobile
        ' interface SmartWach
        OBS: Só se retorna apenas um elemento.  Porque precisamos renderizar tudo do componente em apenas uma <div> ou fragment ( <> </> ).

    - Possui o conceito de componentes
    - Componente:
        - Função JavaScript que retorna a síntaxe "jsx (html & js)"
        - state: uma variável que quando é atualizado/alterada, o componente é renderizado novamente (atualia o o componente)
        - props: dados passados de um Componente Pai para um Componente Filho
        irm - import React from "react"
        sfc - cria o componente

        - Vantagens
            -Reusabilidade de código...
                Criando um componente uma vez e usa quantas vezes forem possíveis na aplicação


==========================================================================
                                App
==========================================================================

- Precisamos de componente para renderizar as tarefas
    state: uma variável que quando é atualizado/alterada, o componente é renderizado novamente (atualia o o componente)

    - props: dados passados de um Componente Pai para um Componente Filho
    irm - import React from "react"
    sfc - creacte the componente